const path=require("path"),fs=require("fs"),https=require("https"),appDebugger=require("../utils/debugger");module.exports=(s,c)=>{const e=path.join(path.resolve(s),"../../nginx/certs"),r="key.pem",t="cert.pem";if(!fs.existsSync(`${e}/${r}`)||!fs.existsSync(`${e}/${t}`))throw appDebugger.error("SSL certificates missing."),new Error("SSL certificates missing.");const i=fs.readFileSync(`${e}/${r}`,"utf8"),n=fs.readFileSync(`${e}/${t}`,"utf8"),o={key:i,cert:n},a=https.createServer(o,c);return appDebugger.info("HTTPS server created in development mode..."),a};
